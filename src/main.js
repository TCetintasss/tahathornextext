const BUILDINGS=[{id:"ironMine",name:"Demir Madeni",description:"Gezegenin ana agir hammadde damarlarini isler.",image:"/assets/building-core.svg",baseDuration:240,cost:{iron:120,crystal:70,gas:20}},{id:"crystalLab",name:"Kristal Laboratuvari",description:"Ileri alasim ve optik uretimi icin kristal akisinin artirir.",image:"/assets/building-core.svg",baseDuration:300,cost:{iron:100,crystal:120,gas:30}},{id:"gasRefinery",name:"Gaz Rafinerisi",description:"Itki, enerji ve yuksek sicaklik surecleri icin gaz aritir.",image:"/assets/building-core.svg",baseDuration:360,cost:{iron:90,crystal:60,gas:110}},{id:"shipyard",name:"Tersane",description:"Ayni anda uretilebilecek gemi sinifi sayisini belirler.",image:"/assets/building-core.svg",baseDuration:540,cost:{iron:180,crystal:140,gas:90}},{id:"repairDock",name:"Tamir Hangari",description:"Hasarli savas filolarini hurda kullanarak toparlar.",image:"/assets/building-core.svg",baseDuration:420,cost:{iron:150,crystal:110,gas:80}},{id:"marketPort",name:"Pazar Limani",description:"Ticaret gemilerinin rota hizini artirir.",image:"/assets/building-core.svg",baseDuration:420,cost:{iron:150,crystal:120,gas:70}},{id:"deepScanner",name:"Derin Tarayici",description:"Kesif menzilini ve gorunurluk suresini uzatir.",image:"/assets/building-core.svg",baseDuration:480,cost:{iron:130,crystal:170,gas:100}},{id:"recycler",name:"Geri Donusum Birimi",description:"Yikilan gemilerden daha yuksek hurda cikma ihtimali saglar.",image:"/assets/building-core.svg",baseDuration:360,cost:{iron:120,crystal:90,gas:60}}];
const SHIPS=[{id:"scout",class:"kesif",name:"Kesif Gemisi",image:"/assets/ship-scout.svg",duration:300,cost:{iron:90,crystal:120,gas:60},description:"Kapali rota agini acar. Yeni gezegenleri kesfeder, mesafeye gore gorunurluk suresi baslatir.",detail:"Kesif menzili Derin Tarayici ile buyur."},{id:"harvester",class:"lojistik",name:"Kaynak Toplama Gemisi",image:"/assets/ship-hauler.svg",duration:360,cost:{iron:140,crystal:80,gas:110},description:"Gittigi hammade gezegeninde demir, kristal, gaz ve nadir altini ayni anda toplar.",detail:"Kaynak tukendikce gezegen haritadan silinir."},{id:"trader",class:"lojistik",name:"Ticaret Gemisi",image:"/assets/ship-hauler.svg",duration:420,cost:{iron:120,crystal:110,gas:120},description:"Klan ici ve ticaret gezegeni arasinda tum kaynak turlerini tasiyabilir.",detail:"Hedefe vardiginda geri cagirilan kadar orada kalir."},{id:"mothership",class:"ozel",name:"Ana Gemi",image:"/assets/ship-warship.svg",duration:720,cost:{iron:420,crystal:300,gas:360,gold:8},description:"Savas ve filo bonus gemisi. Her cag atlandiginda otomatik 1 adet kazanilir.",detail:"5. cag sonunda toplam hedef 5 adettir."},{id:"warLancer",class:"savas",name:"Uzun Menzil Savas Gemisi",image:"/assets/ship-warship.svg",duration:520,cost:{iron:220,crystal:180,gas:170},description:"Dusuk zirhli ama uzak mesafeden yuksek ilk vurus yapan saldiri sinifi.",detail:"Rakip gezegen baskinlarinda oncu olarak kullanilir."},{id:"warBulwark",class:"savas",name:"Agir Zirhli Savas Gemisi",image:"/assets/ship-warship.svg",duration:560,cost:{iron:280,crystal:120,gas:200},description:"Kisa menzilli, yuksek dayanikli savas gemisi. Filonun baskiyi tasiyan govdesidir.",detail:"Tamir Hangari hurda ile bu sinifi onarabilir."},{id:"salvager",class:"destek",name:"Hurda Toplayici",image:"/assets/ship-scout.svg",duration:400,cost:{iron:130,crystal:100,gas:90},description:"Sadece yok olmus veya parcalanmis gemilerden hurda toplar. Hurda uretimi yapmaz.",detail:"Geri Donusum Birimi seviyesi sansi artirir."}];
const AGES=[{name:"Cag I · Koloni",requirements:["Baslangic komuta seviyesi"]},{name:"Cag II · Yorunge",requirements:["Demir Madeni 3","Kristal Laboratuvari 2","Tersane 2"]},{name:"Cag III · Derin Maden",requirements:["Gaz Rafinerisi 3","Derin Tarayici 2","En az 2 kesif gemisi"]},{name:"Cag IV · Cephe",requirements:["Tamir Hangari 2","En az 1 savas gemisi sinifi aktif","3 kesfedilmis gezegen"]},{name:"Cag V · Armada",requirements:["Pazar Limani 3","Geri Donusum Birimi 3","Aktif klan savasi veya 5 ticaret rotasi"]}];
const PLANET_TEMPLATES=[{id:"p1",name:"Khepri-9",type:"resource",distance:6,stock:{iron:1600,crystal:800,gas:540,goldChance:0.02},otherPlayers:["VegaArcanum"],image:"/assets/planet-resource.svg"},{id:"p2",name:"Nysa Delta",type:"resource",distance:12,stock:{iron:1200,crystal:1400,gas:620,goldChance:0.03},otherPlayers:[],image:"/assets/planet-resource.svg"},{id:"p3",name:"Boreal Forge",type:"resource",distance:18,stock:{iron:1900,crystal:600,gas:960,goldChance:0.04},otherPlayers:["AstraNomad","CerberusHex"],image:"/assets/planet-resource.svg"},{id:"p4",name:"Serrin Bastion",type:"rival",distance:24,stock:{iron:0,crystal:0,gas:0,goldChance:0},otherPlayers:["Rival Clan"],image:"/assets/planet-rival.svg"},{id:"p5",name:"Orion Grave",type:"resource",distance:32,stock:{iron:2200,crystal:1500,gas:1400,goldChance:0.05},otherPlayers:["VegaArcanum"],image:"/assets/planet-resource.svg"},{id:"p6",name:"Halcyon Rim",type:"resource",distance:48,stock:{iron:3500,crystal:2400,gas:1900,goldChance:0.07},otherPlayers:["Rival Clan","VegaArcanum"],image:"/assets/planet-resource.svg"}];
const formatNumber=(value)=>new Intl.NumberFormat("tr-TR",{maximumFractionDigits:0}).format(Math.max(0,value));
const formatClock=(timestamp)=>new Intl.DateTimeFormat("tr-TR",{hour:"2-digit",minute:"2-digit",second:"2-digit",timeZone:Intl.DateTimeFormat().resolvedOptions().timeZone}).format(timestamp);
const formatRemaining=(ms)=>{const total=Math.max(0,Math.ceil(ms/1000));const h=Math.floor(total/3600);const m=Math.floor((total%3600)/60);const s=total%60;if(h>0)return`${h}s ${m}dk`;if(m>0)return`${m}dk ${s}sn`;return`${s}sn`;};
const state={authMode:"register",authDraft:{username:"",email:"",password:"",loginUser:"",loginPassword:""},loggedIn:false,currentView:"planet",now:Date.now(),lastTick:Date.now(),user:{username:"komutan_taha",email:"komutan@testtaha.space",password:"123456",planetName:"TestTaha Prime",avatarSeed:"KT",credits:180},resources:{iron:1480,crystal:920,gas:760,gold:14,scrap:0},fleetModules:{goldScanner:false},buildings:{ironMine:2,crystalLab:2,gasRefinery:1,shipyard:1,repairDock:1,marketPort:1,deepScanner:1,recycler:1},buildingQueue:[],shipInventory:{scout:2,harvester:3,trader:2,mothership:1,warLancer:0,warBulwark:0,salvager:1},damagedShips:{warLancer:1,warBulwark:0},mothershipRewardsGranted:1,shipDrafts:Object.fromEntries(SHIPS.map((ship)=>[ship.id,1])),shipQueue:[],fleets:[],marketOrders:[],planets:PLANET_TEMPLATES.map((planet)=>({...planet,discovered:false,visibleUntil:0,playerPresence:0})),clan:{name:"Anka Birligi",role:"Kurucu",warActive:true,rivalClan:"Helix Dominion"},tradeDraft:{target:"tradeHub",shipCount:1,iron:0,crystal:0,gas:0,gold:0},notice:""};
const app=document.querySelector("#root");
const buildingCost=(building)=>{const level=state.buildings[building.id];return Object.fromEntries(Object.entries(building.cost).map(([key,value])=>[key,value*(level+1)]));};
const shipCost=(ship,count)=>Object.fromEntries(Object.entries(ship.cost).map(([key,value])=>[key,value*count]));
const hasCost=(cost)=>Object.entries(cost).every(([key,value])=>key==="credits"?state.user.credits>=value:(state.resources[key]||0)>=value);
const spendCost=(cost)=>Object.entries(cost).forEach(([key,value])=>{if(key==="credits")state.user.credits-=value;else state.resources[key]-=value;});
const addNotice=(message)=>{state.notice=message;render();};
const getAgeIndex=()=>{const b=state.buildings;const discoveredCount=state.planets.filter((planet)=>planet.discovered&&planet.type!=="rival").length;const tradeRoutes=state.fleets.filter((fleet)=>fleet.kind==="trade").length;const conditions=[true,b.ironMine>=3&&b.crystalLab>=2&&b.shipyard>=2,b.gasRefinery>=3&&b.deepScanner>=2&&state.shipInventory.scout>=2,b.repairDock>=2&&(state.shipInventory.warLancer>0||state.shipInventory.warBulwark>0)&&discoveredCount>=3,b.marketPort>=3&&b.recycler>=3&&(state.clan.warActive||tradeRoutes>=5)];let current=0;conditions.forEach((ok,index)=>{if(ok)current=index;});return current;};
const getDiscoveryDuration=(distance)=>{const base={6:6,12:12,18:18,24:24,32:32,48:48}[distance];const scannerBonus=Math.max(0,state.buildings.deepScanner-1)*2;return(base+scannerBonus)*60*60*1000;};
const productionPerHour=()=>{const energyFactor=state.buildings.gasRefinery>=2?1:0.88;return{iron:state.buildings.ironMine*46.8*energyFactor,crystal:state.buildings.crystalLab*31.2*energyFactor,gas:state.buildings.gasRefinery*26.4*energyFactor};};
const shipyardClassLimit=(level)=>level>=2?3:1;
function processTick(deltaMs){const ageIndex=getAgeIndex();if(ageIndex+1>state.mothershipRewardsGranted){const rewardCount=ageIndex+1-state.mothershipRewardsGranted;state.shipInventory.mothership+=rewardCount;state.mothershipRewardsGranted=ageIndex+1;state.notice=`Cag ilerlemesi tamamlandi. ${rewardCount} adet Ana Gemi filoya eklendi.`;}const perHour=productionPerHour();const hours=deltaMs/3600000;state.resources.iron+=perHour.iron*hours;state.resources.crystal+=perHour.crystal*hours;state.resources.gas+=perHour.gas*hours;state.buildingQueue=state.buildingQueue.filter((job)=>{if(state.now>=job.endsAt){state.buildings[job.buildingId]+=1;return false;}return true;});state.shipQueue=state.shipQueue.filter((job)=>{if(state.now>=job.endsAt){state.shipInventory[job.shipId]+=job.count;return false;}return true;});state.fleets=state.fleets.filter((fleet)=>{if(state.now<fleet.arrivesAt)return true;if(fleet.phase==="outbound"){if(fleet.kind==="scout"){const planet=state.planets.find((item)=>item.id===fleet.targetId);if(planet){planet.discovered=true;planet.visibleUntil=state.now+getDiscoveryDuration(planet.distance);planet.playerPresence+=1;}state.shipInventory.scout+=fleet.ships.scout||0;return false;}if(fleet.kind==="trade"){fleet.phase="holding";fleet.holdingAt=state.now;fleet.arrivesAt=state.now+24*60*60*1000;return true;}if(fleet.kind==="harvest"){const planet=state.planets.find((item)=>item.id===fleet.targetId);if(planet){const load={iron:Math.min(planet.stock.iron,fleet.cargoSpace*0.4),crystal:Math.min(planet.stock.crystal,fleet.cargoSpace*0.32),gas:Math.min(planet.stock.gas,fleet.cargoSpace*0.28)};const otherPlayers=planet.otherPlayers.length;planet.stock.iron-=load.iron*(1+otherPlayers*0.18);planet.stock.crystal-=load.crystal*(1+otherPlayers*0.18);planet.stock.gas-=load.gas*(1+otherPlayers*0.18);fleet.cargo=load;if(state.shipInventory.scout>0&&Math.random()<planet.stock.goldChance)fleet.cargo.gold=1;fleet.phase="returning";fleet.arrivesAt=state.now+fleet.travelMs;return true;}return false;}if(fleet.kind==="salvage"){const scrapGain=Math.round(12+Math.random()*18+state.buildings.recycler*4);fleet.cargo={scrap:scrapGain};fleet.phase="returning";fleet.arrivesAt=state.now+fleet.travelMs;return true;}return false;}if(fleet.phase==="returning"){Object.entries(fleet.cargo||{}).forEach(([key,value])=>{if(["scrap","gold","iron","crystal","gas"].includes(key))state.resources[key]+=value;});Object.entries(fleet.ships||{}).forEach(([shipId,count])=>{state.shipInventory[shipId]+=count;});return false;}return true;});state.planets=state.planets.filter((planet)=>{if(!planet.discovered)return true;if(planet.type==="resource"){const empty=planet.stock.iron<=0&&planet.stock.crystal<=0&&planet.stock.gas<=0;if(empty)return false;}if(planet.type!=="rival"&&planet.visibleUntil<state.now){planet.discovered=false;planet.playerPresence=0;}return true;});}
function beginBuildingUpgrade(buildingId){const building=BUILDINGS.find((item)=>item.id===buildingId);const cost=buildingCost(building);if(!hasCost(cost))return addNotice("Yukseltme icin yeterli kaynak yok.");if(state.buildingQueue.some((job)=>job.buildingId===buildingId))return addNotice("Bu bina zaten yukseltiliyor.");spendCost(cost);state.buildingQueue.push({buildingId,startedAt:state.now,endsAt:state.now+building.baseDuration*(state.buildings[buildingId]+1)*1000});addNotice(`${building.name} yukseltmesi baslatildi.`);}
function queueShipProduction(shipId){const ship=SHIPS.find((item)=>item.id===shipId);const count=Math.max(1,Number(state.shipDrafts[shipId]||1));const cost=shipCost(ship,count);const activeClasses=new Set(state.shipQueue.map((job)=>SHIPS.find((item)=>item.id===job.shipId)?.class));const slots=shipyardClassLimit(state.buildings.shipyard);if(!activeClasses.has(ship.class)&&activeClasses.size>=slots)return addNotice(`Tersane seviyesi ${state.buildings.shipyard} iken ayni anda en fazla ${slots} sinif uretilebilir.`);if(!hasCost(cost))return addNotice("Gemi uretimi icin yeterli kaynak yok.");spendCost(cost);state.shipQueue.push({shipId,count,startedAt:state.now,endsAt:state.now+ship.duration*count*1000});addNotice(`${ship.name} icin ${count} adet uretim kuyruga alindi.`);}
function submitAuth(){if(state.authMode==="register"){const{username,email,password}=state.authDraft;if(!username||!email||!password)return addNotice("Kayit icin kullanici adi, mail ve sifre gerekli.");state.user.username=username;state.user.email=email;state.user.password=password;state.user.planetName=`${username} Prime`;state.user.avatarSeed=username.slice(0,2).toUpperCase();state.loggedIn=true;state.notice="Kayit tamamlandi. Kullanici adin oyun ici ismin oldu.";return render();}if(state.authDraft.loginUser===state.user.username&&state.authDraft.loginPassword===state.user.password){state.loggedIn=true;state.notice="Giris yapildi.";}else state.notice="Kullanici adi veya sifre hatali.";render();}
function sendScout(planetId){if(state.shipInventory.scout<=0)return addNotice("Kesif gemin yok.");const planet=state.planets.find((item)=>item.id===planetId);if(!planet)return;state.shipInventory.scout-=1;const travelMs=planet.distance*60*1000;state.fleets.push({id:crypto.randomUUID(),shipId:"scout",label:`Kesif filosu · ${planet.name}`,kind:"scout",ships:{scout:1},targetId:planetId,targetName:planet.name,phase:"outbound",travelMs,startedAt:state.now,arrivesAt:state.now+travelMs,cargo:{}});addNotice(`${planet.name} icin kesif cikisi yapildi.`);}
function sendHarvest(planetId){const planet=state.planets.find((item)=>item.id===planetId);if(!planet||!planet.discovered)return addNotice("Once gezegeni kesfetmen gerekiyor.");if(state.shipInventory.harvester<=0)return addNotice("Kaynak toplama gemin yok.");state.shipInventory.harvester-=1;const travelMs=planet.distance*75*1000;state.fleets.push({id:crypto.randomUUID(),shipId:"harvester",label:`Toplama filosu · ${planet.name}`,kind:"harvest",ships:{harvester:1},targetId:planetId,targetName:planet.name,phase:"outbound",travelMs,startedAt:state.now,arrivesAt:state.now+travelMs,cargoSpace:220,cargo:{}});addNotice(`${planet.name} icin kaynak toplama seferi basladi.`);}
function sendSalvage(planetId){const planet=state.planets.find((item)=>item.id===planetId);if(!planet||!planet.discovered)return addNotice("Hurda toplamak icin once bolge kesfedilmeli.");if(state.shipInventory.salvager<=0)return addNotice("Hurda toplayici gemin yok.");state.shipInventory.salvager-=1;const travelMs=planet.distance*70*1000;state.fleets.push({id:crypto.randomUUID(),shipId:"salvager",label:`Hurda toplama filosu · ${planet.name}`,kind:"salvage",ships:{salvager:1},targetId:planetId,targetName:planet.name,phase:"outbound",travelMs,startedAt:state.now,arrivesAt:state.now+travelMs,cargo:{}});addNotice(`${planet.name} cevresindeki enkaz icin hurda filosu yollandi.`);}
function sendTrade(){const draft=state.tradeDraft;const shipCount=Math.max(1,Number(draft.shipCount||1));if(state.shipInventory.trader<shipCount)return addNotice("Bu ticaret icin yeterli ticaret gemisi yok.");const cargo={iron:Number(draft.iron||0),crystal:Number(draft.crystal||0),gas:Number(draft.gas||0),gold:Number(draft.gold||0)};const total=Object.values(cargo).reduce((sum,value)=>sum+value,0);if(!total)return addNotice("Ticaret icin gonderilecek kaynak secmelisin.");if(!hasCost(cargo))return addNotice("Sectigin kaynak miktari depoda yok.");spendCost(cargo);state.shipInventory.trader-=shipCount;const speedFactor=1-Math.min(0.35,(state.buildings.marketPort-1)*0.08);const travelMs=Math.round(18*60*1000*speedFactor);state.fleets.push({id:crypto.randomUUID(),shipId:"trader",label:"Ticaret filosu · Ticaret Gezegeni",kind:"trade",ships:{trader:shipCount},targetId:"tradeHub",targetName:"Ticaret Gezegeni",phase:"outbound",travelMs,startedAt:state.now,arrivesAt:state.now+travelMs,cargo});state.marketOrders.unshift({id:crypto.randomUUID(),seller:state.user.username,location:"Ticaret Gezegeni",cargo,goldPrice:Math.round(total*0.16)});addNotice("Ticaret filosu ticaret gezegenine cikti. Alislar altin ile gerceklesir.");}
function recallFleet(fleetId){const fleet=state.fleets.find((item)=>item.id===fleetId);if(!fleet||fleet.kind!=="trade"||fleet.phase!=="holding")return;fleet.phase="returning";fleet.arrivesAt=state.now+fleet.travelMs;addNotice("Ticaret filosu geri cagrildi.");}
function renamePlanet(name){if(!name.trim())return addNotice("Gezegen adi bos olamaz.");state.user.planetName=name.trim();addNotice(`Gezegen adi ${state.user.planetName} olarak guncellendi.`);}
function buyCredits(){state.user.credits+=50;addNotice("Ornek premium paket eklendi. Kredi uretimi yok; yalnizca satin alim yuzeyi var.");}
function toggleGoldScanner(){state.fleetModules.goldScanner=!state.fleetModules.goldScanner;addNotice(state.fleetModules.goldScanner?"Altin tarayicisi aktif. Kesif edilmis gezegenlerde dusuk altin sansi acildi.":"Altin tarayicisi kapatildi.");}
function repairShip(shipId){const damaged=state.damagedShips[shipId]||0;if(!damaged)return addNotice("Tamir bekleyen hasarli gemi yok.");const scrapCost=shipId==="warBulwark"?28:22;if(state.resources.scrap<scrapCost)return addNotice("Tamir icin yeterli hurda yok.");state.resources.scrap-=scrapCost;state.damagedShips[shipId]-=1;state.shipInventory[shipId]+=1;addNotice(`${SHIPS.find((ship)=>ship.id===shipId).name} tamir edildi.`);}
const progress=(startedAt,endsAt)=>{const duration=endsAt-startedAt;const elapsed=Math.max(0,Math.min(duration,state.now-startedAt));return duration<=0?100:elapsed/duration*100;};
function renderAuth(){return`<section class="screen auth-shell"><div class="auth-brand"><div><div class="brand-mark">TestTaha Command</div><h1>Uzay madenciliginde canli komuta yuzeyi.</h1><p>Gercek saatle calisan, kesif odakli, ticaret gezegeni ve operasyon takibi guclendirilmis komuta kabugu. Hurda yalnizca yok olan gemilerden gelir, kredi ise yalnizca premium satin alim yuzeyidir.</p><div class="auth-visual" aria-hidden="true"></div></div><small class="muted">Saat dilimi: ${Intl.DateTimeFormat().resolvedOptions().timeZone} · Oyun saati birebir gercek zaman</small></div><div class="auth-panel-wrap"><div class="auth-panel"><div class="auth-switch"><button class="${state.authMode==="register"?"is-active":""}" data-action="switch-auth" data-mode="register">Kayit Ol</button><button class="${state.authMode==="login"?"is-active":""}" data-action="switch-auth" data-mode="login">Giris Yap</button></div><h2>${state.authMode==="register"?"Yeni komutan olustur":"Komuta merkezine gir"}</h2><p class="helper">${state.authMode==="register"?"Kullanici adin ayni zamanda oyun ici ismin olacak.":"Giris icin kullanici adi ve sifre yeterli."}</p>${state.authMode==="register"?`<div class="field"><label>Kullanici adi</label><input data-model="authDraft.username" value="${state.authDraft.username}" placeholder="komutan_taha" /></div><div class="field"><label>Mail</label><input data-model="authDraft.email" value="${state.authDraft.email}" placeholder="mail@ornek.com" /></div><div class="field"><label>Sifre</label><input type="password" data-model="authDraft.password" value="${state.authDraft.password}" placeholder="••••••••" /></div><div class="note">Bu kullanici adi, profilinde ve oyundaki komutan adinda gorunecek.</div>`:`<div class="field"><label>Kullanici adi</label><input data-model="authDraft.loginUser" value="${state.authDraft.loginUser}" placeholder="komutan_taha" /></div><div class="field"><label>Sifre</label><input type="password" data-model="authDraft.loginPassword" value="${state.authDraft.loginPassword}" placeholder="••••••••" /></div>`}<div class="actions"><button class="primary-btn" data-action="submit-auth">${state.authMode==="register"?"Kayit Yap":"Giris Yap"}</button></div>${state.notice?`<p class="small muted">${state.notice}</p>`:""}</div></div></section>`;}
function renderResourceBar(){return`<div class="status-grid"><div class="stat"><small>Demir</small><div class="stat-value">${formatNumber(state.resources.iron)}</div></div><div class="stat"><small>Kristal</small><div class="stat-value">${formatNumber(state.resources.crystal)}</div></div><div class="stat"><small>Gaz</small><div class="stat-value">${formatNumber(state.resources.gas)}</div></div><div class="stat"><small>Altin</small><div class="stat-value">${formatNumber(state.resources.gold)}</div></div><div class="stat"><small>Hurda</small><div class="stat-value">${formatNumber(state.resources.scrap)}</div><small class="warning">Uretim yok. Sadece yikilan gemilerden gelir.</small></div><div class="stat"><small>Kredi</small><div class="stat-value">${formatNumber(state.user.credits)}</div><small class="warning">Uretim yok. Sadece satin alim.</small></div></div>`;}
function renderHero(){const ageIndex=getAgeIndex();const nextAge=AGES[Math.min(ageIndex+1,AGES.length-1)];const slots=shipyardClassLimit(state.buildings.shipyard);return`<section class="hero"><div class="panel"><small class="muted">Ana gezegen</small><h2>${state.user.planetName}</h2><p class="muted">Gercek zamanli komuta yuzeyi. Kesif yapilmadan rota acilmaz, ticaret gemileri hedefte bekler, pazar alislari yalnizca altin ile doner.</p>${renderResourceBar()}</div><div class="panel"><div class="section-title"><div><small class="muted">Cag durumu</small><h3>${AGES[ageIndex].name}</h3></div><span class="chip">Tersane slotu ${slots}</span></div><p class="muted">Sonraki cag icin gerekenler oyuncuya acik gorunuyor.</p><div class="item"><strong>${nextAge.name}</strong><div class="chips">${nextAge.requirements.map((requirement)=>`<span class="chip">${requirement}</span>`).join("")}</div></div></div></section>`;}
function renderBuildings(){return`<div class="panel"><div class="section-title"><div><small class="muted">Yukseltmeler</small><h3>Bina agi</h3></div><span class="chip">Barli zaman takibi</span></div><div class="list">${BUILDINGS.map((building)=>{const queueJob=state.buildingQueue.find((job)=>job.buildingId===building.id);const cost=buildingCost(building);return`<article class="item"><div class="item-top"><div class="item-title"><img src="${building.image}" alt="${building.name}" /><div><h4>${building.name} · Seviye ${state.buildings[building.id]}</h4><p class="muted">${building.description}</p></div></div><button class="secondary-btn" data-action="upgrade-building" data-building-id="${building.id}">Yukselt</button></div><div class="chips">${Object.entries(cost).map(([key,value])=>`<span class="chip">${key}: ${formatNumber(value)}</span>`).join("")}</div>${queueJob?`<div class="meta-grid"><div class="meta-box"><small class="muted">Kalan sure</small><div>${formatRemaining(queueJob.endsAt-state.now)}</div></div><div class="meta-box"><small class="muted">Ilerleme</small><div class="progress"><span style="width:${progress(queueJob.startedAt,queueJob.endsAt)}%"></span></div></div></div>`:""}</article>`;}).join("")}</div></div>`;}
function renderShips(){const slots=shipyardClassLimit(state.buildings.shipyard);return`<div class="panel"><div class="section-title"><div><small class="muted">Filo uretimi</small><h3>Gemi tersanesi</h3></div><span class="chip">Ayni anda ${slots} sinif</span></div><div class="list">${SHIPS.map((ship)=>{const count=state.shipDrafts[ship.id];const queueJob=state.shipQueue.find((item)=>item.shipId===ship.id);return`<article class="item"><div class="item-top"><div class="item-title"><img src="${ship.image}" alt="${ship.name}" /><div><h4>${ship.name}</h4><p class="muted">${ship.description}</p><div class="chips"><span class="chip">Sinif: ${ship.class}</span><span class="chip">Envanter: ${state.shipInventory[ship.id]}</span><span class="chip">Uretim: ${Math.round(ship.duration/60)} dk / adet</span></div><p class="small muted">${ship.detail}</p></div></div><button class="primary-btn" data-action="queue-ship" data-ship-id="${ship.id}">Uretime Al</button></div><div class="count-picker"><input type="number" min="1" max="20" value="${count}" data-model="shipDrafts.${ship.id}" /><input type="range" min="1" max="20" value="${count}" data-model="shipDrafts.${ship.id}" /></div><div class="chips">${Object.entries(shipCost(ship,count)).map(([key,value])=>`<span class="chip">${key}: ${formatNumber(value)}</span>`).join("")}</div>${queueJob?`<div class="meta-grid"><div class="meta-box"><small class="muted">Sayac</small><div>${queueJob.count} adet · ${formatRemaining(queueJob.endsAt-state.now)}</div></div><div class="meta-box"><small class="muted">Ilerleme bari</small><div class="progress"><span style="width:${progress(queueJob.startedAt,queueJob.endsAt)}%"></span></div></div></div>`:""}</article>`;}).join("")}</div></div>`;}
function renderAgeLadder(){const ageIndex=getAgeIndex();return`<div class="panel"><div class="section-title"><div><small class="muted">Cag merdiveni</small><h3>Gecis sartlari</h3></div></div><div class="age-ladder">${AGES.map((age,index)=>`<div class="age-step ${index===ageIndex?"is-current":""} ${index>ageIndex?"is-locked":""}"><strong>${age.name}</strong><div class="chips">${age.requirements.map((item)=>`<span class="chip">${item}</span>`).join("")}</div></div>`).join("")}</div></div>`;}
function renderPlanetView(){return`${renderHero()}<section class="layout-grid two-col">${renderBuildings()}<div class="layout-grid">${renderShips()}${renderAgeLadder()}<div class="panel"><div class="section-title"><div><small class="muted">Tamir hangari</small><h3>Hurda ile gemi onarimi</h3></div><span class="chip">Bina seviyesi ${state.buildings.repairDock}</span></div><div class="list">${["warLancer","warBulwark"].map((shipId)=>`<article class="item"><div class="row space"><div><strong>${SHIPS.find((ship)=>ship.id===shipId).name}</strong><div class="small muted">Hasarli adet: ${state.damagedShips[shipId]}</div></div><button class="secondary-btn" data-action="repair-ship" data-ship-id="${shipId}">Hurda ile Tamir Et</button></div></article>`).join("")}</div></div></div></section>`;}
function renderExplorationList(){return`<div class="panel"><div class="section-title"><div><small class="muted">Kesif rotalari</small><h3>Once kesfet, sonra gorev ac</h3></div><span class="chip">Ticaret gezegeni ayri sekmede</span></div><div class="list">${state.planets.map((planet)=>{const visible=planet.discovered&&(planet.type==="rival"||planet.visibleUntil>state.now);const canHarvest=visible&&planet.type==="resource";return`<article class="item"><div class="item-top"><div class="item-title"><img src="${planet.image}" alt="${planet.name}" /><div><h4>${planet.name}</h4><p class="muted">${visible?planet.type==="resource"?"Hammade gezegeni kesfedildi. Kaynak tukenirse haritadan silinir.":"Rakip gezegen kesfedildi. Klan savasi suresince gorunur.":"Rota kapali. Kesif gemisi gondermeden gorev acilmaz."}</p><div class="chips"><span class="chip">Mesafe: ${planet.distance} saat segmenti</span>${visible?`<span class="chip">${planet.type==="resource"?`Gorunurluk: ${formatRemaining(planet.visibleUntil-state.now)}`:"Klan savasi gorunurlugu aktif"}</span>`:`<span class="chip">Gorev: kapali</span>`}${visible?`<span class="chip">Diger oyuncular: ${planet.otherPlayers.join(", ")||"Yok"}</span>`:""}</div></div></div><div class="row">${!visible?`<button class="secondary-btn" data-action="send-scout" data-planet-id="${planet.id}">Kesif Gonder</button>`:""}${canHarvest?`<button class="primary-btn" data-action="send-harvest" data-planet-id="${planet.id}">Toplama Gonder</button>`:""}${canHarvest?`<button class="secondary-btn" data-action="send-salvage" data-planet-id="${planet.id}">Hurda Topla</button>`:""}</div></div>${visible&&planet.type==="resource"?`<div class="meta-grid"><div class="meta-box"><small class="muted">Kalan rezerv</small><div>Demir ${formatNumber(planet.stock.iron)} · Kristal ${formatNumber(planet.stock.crystal)} · Gaz ${formatNumber(planet.stock.gas)}</div></div><div class="meta-box"><small class="muted">Altin sansi</small><div>${Math.round(planet.stock.goldChance*100)}% · Altin tarayicisi ${state.fleetModules.goldScanner?"acik":"kapali"}</div></div></div>`:""}</article>`;}).join("")}</div></div>`;}
function renderMarketView(){const tradeFleets=state.fleets.filter((fleet)=>fleet.kind==="trade");return`${renderHero()}<section class="layout-grid two-col"><div class="panel trade-form"><div class="section-title"><div><small class="muted">Ticaret gezegeni</small><h3>Kaynak gonderimi</h3></div><span class="chip">Alis yalnizca altin ile</span></div><p class="muted">Ticaret gemisini yollarken kaynak miktarini secersin. Hedefe vardiginda geri donmez, geri cagirilmadan o gezegende kalir.</p><div class="field"><label>Ticaret gemisi adedi</label><input type="number" min="1" max="${Math.max(1,state.shipInventory.trader)}" value="${state.tradeDraft.shipCount}" data-model="tradeDraft.shipCount" /></div><div class="resource-fields">${["iron","crystal","gas","gold"].map((resource)=>`<div class="field"><label>${resource}</label><input type="number" min="0" value="${state.tradeDraft[resource]}" data-model="tradeDraft.${resource}" /></div>`).join("")}</div><div class="actions"><button class="primary-btn" data-action="send-trade">Ticaret Gemisi Yolla</button><button class="secondary-btn" data-action="toggle-gold-scanner">Altin Tarayicisi ${state.fleetModules.goldScanner?"Acik":"Kapali"}</button></div></div><div class="panel"><div class="section-title"><div><small class="muted">Pazar + ticaret entegrasyonu</small><h3>Ticaret gemileri</h3></div></div><div class="list"><article class="item"><h4>Durum ozeti</h4><div class="chips"><span class="chip">Toplam ticaret gemisi: ${state.shipInventory.trader+tradeFleets.reduce((sum,fleet)=>sum+fleet.ships.trader,0)}</span><span class="chip">Yolda: ${tradeFleets.filter((fleet)=>fleet.phase==="outbound").length}</span><span class="chip">Gezegende bekleyen: ${tradeFleets.filter((fleet)=>fleet.phase==="holding").length}</span><span class="chip">Donen: ${tradeFleets.filter((fleet)=>fleet.phase==="returning").length}</span></div></article>${tradeFleets.map((fleet)=>`<article class="item"><div class="row space"><strong>${fleet.label}</strong><span class="chip">${fleet.phase==="outbound"?`Varis ${formatRemaining(fleet.arrivesAt-state.now)}`:fleet.phase==="holding"?"Ticaret gezegeninde bekliyor":`Donus ${formatRemaining(fleet.arrivesAt-state.now)}`}</span></div><div class="cargo-list">${Object.entries(fleet.cargo||{}).filter(([,value])=>value).map(([key,value])=>`<span>${key}: ${formatNumber(value)}</span>`).join("")}</div>${fleet.phase==="holding"?`<div class="actions"><button class="secondary-btn" data-action="recall-fleet" data-fleet-id="${fleet.id}">Geri Cagir</button></div>`:""}</article>`).join("")}</div></div></section><section class="layout-grid"><div class="panel"><div class="section-title"><div><small class="muted">Altin bazli emirler</small><h3>Pazar emirleri</h3></div></div><div class="list">${state.marketOrders.map((order)=>`<article class="item"><div class="row space"><div><strong>${order.seller}</strong><div class="small muted">${order.location}</div></div><span class="chip">${order.goldPrice} altin</span></div><div class="cargo-list">${Object.entries(order.cargo).filter(([,value])=>value).map(([key,value])=>`<span>${key}: ${formatNumber(value)}</span>`).join("")}</div></article>`).join("")||`<div class="item"><p class="muted">Henuz ticaret gezegenine aktif ilan cikilmadi.</p></div>`}</div></div></section>`;}
function renderOperationsView(){const fleets=state.fleets;return`${renderHero()}<section class="layout-grid"><div class="panel"><div class="section-title"><div><small class="muted">Operasyon sayfasi</small><h3>Gidis gelis zamanlari</h3></div></div><div class="timeline">${fleets.map((fleet)=>`<article class="fleet-card"><div class="fleet-header"><div><strong>${fleet.label}</strong><div class="small muted">Filo blok gorunumu · ${Object.entries(fleet.ships).map(([ship,count])=>`${SHIPS.find((item)=>item.id===ship).name} x${count}`).join(", ")}</div></div><span class="chip">${fleet.phase==="outbound"?`Varisa ${formatRemaining(fleet.arrivesAt-state.now)}`:fleet.phase==="holding"?"Hedefte bekliyor":`Donuse ${formatRemaining(fleet.arrivesAt-state.now)}`}</span></div><div class="meta-grid"><div class="meta-box"><small class="muted">Hedef</small><div>${fleet.targetName}</div></div><div class="meta-box"><small class="muted">Tasidigi hammadde</small><div>${Object.entries(fleet.cargo||{}).filter(([,value])=>value).map(([key,value])=>`${key} ${formatNumber(value)}`).join(" · ")||"Yok"}</div></div></div><div style="margin-top:12px" class="progress"><span style="width:${progress(fleet.startedAt,fleet.arrivesAt)}%"></span></div></article>`).join("")||`<div class="item"><p class="muted">Aktif sefer yok. Gonderilen filolar burada detayli gorunur.</p></div>`}</div></div></section>`;}
function renderMapView(){const basePoints=[{id:"home",x:200,y:230,label:state.user.planetName,fill:"#66d9c2"},{id:"trade",x:600,y:120,label:"Ticaret Gezegeni",fill:"#f3c969"}];const discovered=state.planets.filter((planet)=>planet.discovered||(planet.type==="rival"&&state.clan.warActive)).map((planet,index)=>({id:planet.id,x:420+Math.cos(index*1.2)*(120+planet.distance*3),y:220+Math.sin(index*1.2)*(90+planet.distance*2),label:planet.name,fill:planet.type==="rival"?"#ff7f88":"#7dafff",other:planet.otherPlayers.join(", "),distance:planet.distance}));const fleets=state.fleets.map((fleet,index)=>({id:fleet.id,x:260+index*78,y:340+(index%2)*46,label:`${fleet.targetName} · ${formatRemaining(fleet.arrivesAt-state.now)}`}));return`${renderHero()}<section class="layout-grid two-col"><div class="panel map-shell"><div class="section-title"><div><small class="muted">Harita</small><h3>Kesif edilen gezegenler ve rotalar</h3></div></div><div class="map-board"><svg viewBox="0 0 800 500" role="img" aria-label="Galaksi haritasi"><defs><filter id="glow"><feGaussianBlur stdDeviation="6" result="blur"></feGaussianBlur><feMerge><feMergeNode in="blur"></feMergeNode><feMergeNode in="SourceGraphic"></feMergeNode></feMerge></filter></defs>${[...basePoints,...discovered].map((point)=>`<circle cx="${point.x}" cy="${point.y}" r="22" fill="${point.fill}" opacity="0.18" filter="url(#glow)"></circle><circle cx="${point.x}" cy="${point.y}" r="10" fill="${point.fill}"></circle><text x="${point.x+18}" y="${point.y-10}" fill="#eef7fc" font-size="16" font-family="Segoe UI">${point.label}</text>${point.other?`<text x="${point.x+18}" y="${point.y+12}" fill="#8ca7b7" font-size="12" font-family="Segoe UI">Oyuncular: ${point.other}</text>`:""}`).join("")}${fleets.map((fleet)=>`<circle cx="${fleet.x}" cy="${fleet.y}" r="7" fill="#ffffff"></circle><text x="${fleet.x+12}" y="${fleet.y+4}" fill="#ffffff" font-size="12" font-family="Segoe UI">${fleet.label}</text>`).join("")}</svg></div><div class="legend"><span><i class="dot" style="background:#66d9c2"></i>Kendi gezegenin</span><span><i class="dot" style="background:#f3c969"></i>Ticaret gezegeni</span><span><i class="dot" style="background:#7dafff"></i>Kesif edilen hammade gezegeni</span><span><i class="dot" style="background:#ff7f88"></i>Rakip gezegen / savas gorunurlugu</span><span><i class="dot" style="background:#ffffff"></i>Yoldaki filo</span></div></div>${renderExplorationList()}</section>`;}
function renderClanView(){return`${renderHero()}<section class="layout-grid two-col"><div class="panel"><div class="section-title"><div><small class="muted">Klan ve savas</small><h3>Rakip istihbarati</h3></div></div><div class="item"><h4>${state.clan.name}</h4><p class="muted">Rol: ${state.clan.role} · Aktif savas: ${state.clan.warActive?"Var":"Yok"}</p><div class="chips"><span class="chip">Rakip klan: ${state.clan.rivalClan}</span><span class="chip">Savas surerken rakip gezegenleri gorunur kalir</span></div></div><div class="item"><h4>Savas gemileri</h4><p class="muted">Destek gemisi adi kaldirildi. Iki savas sinifi var: uzun menzil ve agir zirhli.</p><div class="chips"><span class="chip">Uzun menzil: ${state.shipInventory.warLancer}</span><span class="chip">Agir zirhli: ${state.shipInventory.warBulwark}</span><span class="chip">Ana gemi: ${state.shipInventory.mothership}</span><span class="chip">Hasarli uzun menzil: ${state.damagedShips.warLancer}</span><span class="chip">Hasarli agir zirhli: ${state.damagedShips.warBulwark}</span></div></div></div><div class="panel"><div class="section-title"><div><small class="muted">Kesif gorunurlugu</small><h3>Mesafe bazli sureler</h3></div></div><div class="age-ladder">${[6,12,18,24,32,48].map((distance)=>`<div class="age-step"><strong>${distance} saat segmenti</strong><div class="small muted">Gorunur kalma: ${distance} saat + Derin Tarayici bonusu</div></div>`).join("")}</div></div></section>`;}
function renderProfileView(){return`${renderHero()}<section class="profile-page"><div class="panel"><div class="section-title"><div><small class="muted">Profil</small><h3>Komutan bilgileri</h3></div></div><div class="item"><div class="row"><div class="avatar">${state.user.avatarSeed}</div><div><h4>${state.user.username}</h4><div class="small muted">${state.user.email}</div></div></div></div><div class="item"><h4>Gezegen adi</h4><div class="inline-form"><input id="planet-rename-input" value="${state.user.planetName}" /><button class="primary-btn" data-action="rename-planet">Adi Guncelle</button></div></div><div class="item"><h4>Premium kredi</h4><p class="muted">Kredi uretimi kapali. Bu alan sadece satin alim yuzeyi olarak tutuldu.</p><div class="actions"><button class="primary-btn" data-action="buy-credits">50 Kredi Satin Al (ornek)</button></div></div></div></section>`;}
function renderApp(){const viewContent={planet:renderPlanetView(),market:renderMarketView(),operations:renderOperationsView(),map:renderMapView(),clan:renderClanView(),profile:renderProfileView()}[state.currentView];return`<section class="screen app-shell"><aside class="sidebar"><button class="profile-chip nav-btn ${state.currentView==="profile"?"is-active":""}" data-action="go-view" data-view="profile"><div class="avatar">${state.user.avatarSeed}</div><div><strong>${state.user.username}</strong><div class="small muted">${state.user.planetName}</div></div><span class="chip">Profil</span></button><nav>${[["planet","Gezegen"],["market","Pazar"],["operations","Operasyon"],["map","Harita"],["clan","Savas / Klan"]].map(([id,label])=>`<button class="nav-btn ${state.currentView===id?"is-active":""}" data-action="go-view" data-view="${id}">${label}</button>`).join("")}</nav><div class="sidebar-foot"><div class="clock-block"><small class="muted">Gercek oyun saati</small><div class="clock-value">${formatClock(state.now)}</div><small class="muted">${Intl.DateTimeFormat().resolvedOptions().timeZone} ile birebir senkron</small></div><div class="note">Botlar gorunmez. Oyuncu yuzeyinde bot sayfasi yok, ekonomi oyuncuya dogal akis gibi gorunur.</div>${state.notice?`<div class="small muted">${state.notice}</div>`:""}</div></aside><main class="main">${viewContent}</main></section>`;}
function render(){app.innerHTML=state.loggedIn?renderApp():renderAuth();}
function setByPath(path,value){const keys=path.split(".");let ref=state;while(keys.length>1)ref=ref[keys.shift()];const leaf=keys[0];if(typeof ref[leaf]==="number")ref[leaf]=Number(value);else ref[leaf]=value;}
document.addEventListener("click",(event)=>{const target=event.target.closest("[data-action]");if(!target)return;const{action}=target.dataset;if(action==="switch-auth"){state.authMode=target.dataset.mode;state.notice="";}if(action==="submit-auth")submitAuth();if(action==="go-view")state.currentView=target.dataset.view;if(action==="upgrade-building")beginBuildingUpgrade(target.dataset.buildingId);if(action==="queue-ship")queueShipProduction(target.dataset.shipId);if(action==="send-scout")sendScout(target.dataset.planetId);if(action==="send-harvest")sendHarvest(target.dataset.planetId);if(action==="send-salvage")sendSalvage(target.dataset.planetId);if(action==="send-trade")sendTrade();if(action==="recall-fleet")recallFleet(target.dataset.fleetId);if(action==="rename-planet"){const input=document.querySelector("#planet-rename-input");renamePlanet(input.value);}if(action==="buy-credits")buyCredits();if(action==="toggle-gold-scanner")toggleGoldScanner();if(action==="repair-ship")repairShip(target.dataset.shipId);render();});
document.addEventListener("input",(event)=>{const target=event.target;if(!target.dataset.model)return;setByPath(target.dataset.model,target.value);render();});
setInterval(()=>{const now=Date.now();const delta=now-state.lastTick;state.now=now;state.lastTick=now;processTick(delta);render();},1000);
render();